functions

.pop() delete the last element from an array and return its value
.unshift() insert new element at the start of an array
.push() at the end
.match() will return an array of match results – containing either the first match, or all matches if the global flag is used.